# 双球面约束下的机器人运动学误差辨识与补偿方法

## 摘要

本文给出一种基于“内层实体球面—外层虚拟球面”双重几何约束的机器人运动学误差辨识与补偿方案。线激光扫描仪安装于机器人末端，在标准球位于多个万向节姿态时，从多个机器人扫描位姿采集球面点云。每个标准球位置对应一个不可直接测得的实体球球心；这些球心随万向节转动形成外层虚拟球面，其虚拟球心同样不可直接测得。因此，将各实体球球心、虚拟球心以及必要时的虚拟球半径视为隐变量，与机器人修正 MDH 参数共同估计。实际计算采用“两阶段初始化、最终联合优化”的策略：先分别拟合实体球心和虚拟球面以获得稳定初值，再利用内层点到实体球面的残差和外层球心到虚拟球面的残差进行加权 Levenberg–Marquardt（LM）辨识。最后更新机器人几何模型，实施控制器参数更新或离线关节指令补偿，并用与建模数据相互独立的数据验证补偿效果。

---

## 1. 研究目标与核心思想

### 1.1 研究目标

本方法的目标是利用线激光扫描仪和高精度标准球构造几何闭环约束，在不直接测量实体球球心和万向节旋转中心的条件下，辨识机器人名义运动学参数与真实参数之间的偏差，并据此降低机器人末端的绝对定位误差。

主要任务包括：

1. 建立从线激光原始点到机器人基坐标系的完整数据链；
2. 通过已知半径的实体标准球构造内层球面约束；
3. 通过不同实体球球心的运动轨迹构造外层虚拟球面约束；
4. 将无法直接测量的实体球心和虚拟球心作为隐变量；
5. 分析并筛选可辨识的 MDH 参数组合；
6. 采用加权、稳健的 LM 方法联合辨识运动学参数和几何隐变量；
7. 更新运动学模型或修正关节指令，并通过独立数据验证补偿效果。

### 1.2 双球面约束的含义

双球面并不是两个均可直接测量的实体球面，而是两层不同来源的几何约束：

- **内层实体球面**：线激光采集的点应落在半径已知的高精度标准球表面；
- **外层虚拟球面**：当标准球随万向节绕固定中心转动时，不同姿态下的实体球球心应落在同一个虚拟球面上。

内层约束直接作用于原始点云，外层约束作用于多个实体球心。实体球心是连接两层约束的中间变量。相比“先拟合球心、再把球心当作无误差观测量”的串联方法，联合模型能够保留原始点云信息，并把球心估计误差传递到最终参数协方差中。

---

## 2. 实验装置构型与数据采集

### 2.1 装置构型

建议的实验系统包括：

- 一台串联工业机器人；
- 安装在机器人末端法兰上的线激光扫描仪；
- 已知标称半径且具有球度、半径检定值的高精度标准球；
- 可使标准球改变方向、但具有近似固定转动中心的万向节或球铰装置；
- 用于同步记录机器人关节角和线激光轮廓数据的采集系统；
- 必要时配置温度传感器、触发器以及外部高精度测量设备，用于误差监控和独立验证。

### 2.2 推荐的数据组织方式

定义三级索引：

\[
k=1,\ldots,K
\]

表示万向节的第 \(k\) 个姿态，即实体球球心在外层虚拟球面上的第 \(k\) 个位置；

\[
i=1,\ldots,M_k
\]

表示第 \(k\) 个球位置下机器人的第 \(i\) 个扫描位姿；

\[
j=1,\ldots,N_{ki}
\]

表示该扫描帧中的第 \(j\) 个有效激光点。

相应地，每条数据至少应保存：

\[
\left\{k,\ i,\ j,\ \mathbf q_{ki},\ {}^{L}\mathbf p_{kij},\ t_{ki},\ \text{质量标记}\right\},
\]

其中 \(\mathbf q_{ki}\) 为机器人关节角，\({}^{L}\mathbf p_{kij}\) 为激光坐标系中的原始三维点，\(t_{ki}\) 为时间戳。质量标记可记录反光饱和、边缘点、拟合置信度和异常帧等信息。

### 2.3 采样原则

为了提高参数可辨识性，采样时应同时保证：

- 每个实体球位置由多个差异明显的机器人姿态扫描；
- 各关节在允许范围内充分变化，避免所有姿态集中在局部区域；
- 激光覆盖球面的多个方位，避免只采到很小的球冠或近似圆弧；
- 外层虚拟球面上的球心位置具有足够大的角度分布，避免集中在一个小区域或近似共圆；
- 标定集与验证集在采集前即分开设计，防止事后挑选造成数据泄漏。

---

## 3. 坐标系、参数与数据链

### 3.1 坐标系定义

- \(\{B\}\)：机器人基坐标系；
- \(\{E\}\)：机器人末端法兰坐标系；
- \(\{L\}\)：线激光扫描仪坐标系；
- \(\{S_k\}\)：第 \(k\) 个实体球位置对应的辅助坐标系，其原点可取为实体球心；
- \(\{V\}\)：外层虚拟球的辅助坐标系，其原点可取为虚拟球心。

### 3.2 主要符号

| 符号 | 含义 |
|---|---|
| \(\mathbf q_{ki}\) | 第 \(k\) 个球位置、第 \(i\) 个扫描位姿的关节角向量 |
| \(\boldsymbol\eta_0\) | 机器人名义 MDH 参数向量 |
| \(\Delta\boldsymbol\eta\) | 待辨识的 MDH 参数修正量 |
| \(\boldsymbol\eta=\boldsymbol\eta_0+\Delta\boldsymbol\eta\) | 更新后的机器人运动学参数 |
| \({}^{B}\mathbf T_E(\mathbf q,\boldsymbol\eta)\) | 由机器人运动学模型给出的末端到基坐标系变换 |
| \({}^{E}\mathbf T_L\) | 线激光坐标系到末端法兰坐标系的手眼变换 |
| \({}^{L}\mathbf p_{kij}\) | 激光坐标系中的原始点 |
| \({}^{B}\mathbf p_{kij}\) | 转换到基坐标系后的点 |
| \(r_s\) | 高精度标准球半径，通常取检定值并固定 |
| \(\mathbf c_k\) | 第 \(k\) 个实体球位置的球心，属于隐变量 |
| \(\mathbf o\) | 外层虚拟球心，即理想万向节转动中心，属于隐变量 |
| \(R_v\) | 外层虚拟球半径，可固定为已知值或作为隐变量估计 |

### 3.3 数据链

完整数据链为：

\[
\text{激光轮廓原始数据}
\rightarrow {}^{L}\mathbf p_{kij}
\rightarrow {}^{E}\mathbf T_L
\rightarrow {}^{B}\mathbf T_E(\mathbf q_{ki},\boldsymbol\eta)
\rightarrow {}^{B}\mathbf p_{kij}
\rightarrow \text{内层球面残差}
\rightarrow \mathbf c_k
\rightarrow \text{外层球面残差}
\rightarrow \Delta\boldsymbol\eta.
\]

计算上虽然可以按此链条逐级获得初值，但最终不应把前一级结果视为完全无误差的固定真值，而应进行联合优化。

---

## 4. 机器人 MDH 运动学模型

对于第 \(l\) 个关节，采用一类常见的修正 DH（MDH）变换形式：

\[
{}^{l-1}\mathbf T_l
=
\operatorname{Rot}_x(\alpha_{l-1})
\operatorname{Trans}_x(a_{l-1})
\operatorname{Rot}_z(\theta_l)
\operatorname{Trans}_z(d_l),
\]

其中，对于转动关节，

\[
\theta_l=q_l+\theta_{0,l}+\delta\theta_l.
\]

这里：

- \(a_{l-1}\) 为相邻关节轴之间的公法线长度；
- \(\alpha_{l-1}\) 为相邻关节轴扭转角；
- \(d_l\) 为沿第 \(l\) 个关节轴的偏距；
- \(\theta_{0,l}\) 为名义零位偏置；
- \(\delta\theta_l\) 为待辨识的关节零位修正量。

六轴机器人的正运动学写成

\[
{}^{B}\mathbf T_E(\mathbf q,\boldsymbol\eta)
=
{}^{B}\mathbf T_0
\prod_{l=1}^{6}{}^{l-1}\mathbf T_l(q_l,\boldsymbol\eta_l)
{}^{6}\mathbf T_E.
\]

具体 MDH 乘法顺序必须与论文采用的参数表和机器人控制器定义一致。不同文献的“标准 DH/修正 DH”写法可能不同，不能只凭符号名称混用。

将全部候选几何修正量组成

\[
\Delta\boldsymbol\eta
=
\begin{bmatrix}
\Delta a_0,\ldots,\Delta a_5,
\Delta d_1,\ldots,\Delta d_6,
\Delta\alpha_0,\ldots,\Delta\alpha_5,
\Delta\theta_{0,1},\ldots,\Delta\theta_{0,6}
\end{bmatrix}^{T},
\]

但最终进入优化的参数不一定是全部候选参数，应先通过可辨识性分析确定独立参数子集。

---

## 5. 线激光原始点到机器人基坐标系的变换

### 5.1 激光坐标系中的点

线激光经过传感器内部标定后输出三维点：

\[
{}^{L}\mathbf p_{kij}
=
\begin{bmatrix}
x^{L}_{kij} & y^{L}_{kij} & z^{L}_{kij}
\end{bmatrix}^{T}.
\]

若扫描平面被定义为 \(y_L=0\)，则点可写为 \([x^{L}_{kij},0,z^{L}_{kij}]^T\)。其齐次形式为

\[
{}^{L}\bar{\mathbf p}_{kij}
=
\begin{bmatrix}
({}^{L}\mathbf p_{kij})^{T} & 1
\end{bmatrix}^{T}.
\]

### 5.2 手眼变换

扫描仪到末端法兰的刚体变换为

\[
{}^{E}\mathbf T_L
=
\begin{bmatrix}
{}^{E}\mathbf R_L & {}^{E}\mathbf t_L\\
\mathbf 0^T & 1
\end{bmatrix}.
\]

若手眼外参已经过更高精度的独立标定，可在本次辨识中固定。若其精度不足，可将其六维微小位姿修正量加入联合优化，但必须重新做可辨识性分析，因为手眼误差与部分机器人几何误差会高度耦合。

### 5.3 基坐标系中的点

第 \(k,i,j\) 个点在基坐标系中的齐次坐标为

\[
{}^{B}\bar{\mathbf p}_{kij}(\boldsymbol\eta)
=
{}^{B}\mathbf T_E(\mathbf q_{ki},\boldsymbol\eta)
{}^{E}\mathbf T_L
{}^{L}\bar{\mathbf p}_{kij}.
\tag{1}
\]

取前三个分量即可得到

\[
{}^{B}\mathbf p_{kij}(\boldsymbol\eta)
=
{}^{B}\mathbf R_E(\mathbf q_{ki},\boldsymbol\eta)
\left({}^{E}\mathbf R_L{}^{L}\mathbf p_{kij}+{}^{E}\mathbf t_L\right)
+{}^{B}\mathbf t_E(\mathbf q_{ki},\boldsymbol\eta).
\tag{2}
\]

因此，每个基坐标系点均显式依赖机器人关节角、机器人运动学参数和手眼外参。机器人运动学误差会表现为：同一个物理球面从不同机器人姿态观测后，点云不能在基坐标系中正确重合。

### 5.4 小误差线性化

在当前参数估计 \(\boldsymbol\eta^{(t)}\) 附近，点坐标可近似为

\[
{}^{B}\mathbf p_{kij}(\boldsymbol\eta^{(t)}+\Delta\boldsymbol\eta)
\approx
{}^{B}\mathbf p_{kij}(\boldsymbol\eta^{(t)})
+\mathbf J^{p}_{kij}\Delta\boldsymbol\eta,
\]

其中

\[
\mathbf J^{p}_{kij}
=
\frac{\partial {}^{B}\mathbf p_{kij}}{\partial\boldsymbol\eta}
\]

为点坐标对 MDH 参数的灵敏度矩阵。实际实现中可使用解析微分、自动微分或经过数值校验的中心差分计算。

---

## 6. 内层实体球面残差

### 6.1 几何约束

第 \(k\) 个位置的实体球球心记为

\[
\mathbf c_k=[c_{x,k},c_{y,k},c_{z,k}]^T.
\]

标准球半径的检定值为 \(r_s\)。理想情况下，所有属于该球面的点满足

\[
\left\|{}^{B}\mathbf p_{kij}(\boldsymbol\eta)-\mathbf c_k\right\|_2=r_s.
\]

因此定义点到实体球面的有符号径向残差：

\[
e^{s}_{kij}
=
\left\|{}^{B}\mathbf p_{kij}(\boldsymbol\eta)-\mathbf c_k\right\|_2-r_s.
\tag{3}
\]

该残差具有明确的长度单位和几何意义，比直接最小化展开后的代数球方程更适合高精度辨识。

### 6.2 残差雅可比

令

\[
\mathbf n_{kij}
=
\frac{{}^{B}\mathbf p_{kij}-\mathbf c_k}
{\left\|{}^{B}\mathbf p_{kij}-\mathbf c_k\right\|_2},
\]

则有

\[
\frac{\partial e^{s}_{kij}}{\partial\boldsymbol\eta}
=\mathbf n_{kij}^{T}\mathbf J^{p}_{kij},
\qquad
\frac{\partial e^{s}_{kij}}{\partial\mathbf c_k}
=-\mathbf n_{kij}^{T}.
\tag{4}
\]

这表明单个球面点主要约束点误差在球面法向上的分量；只有扫描方位、机器人姿态和球位置足够多样时，参数灵敏度矩阵才能获得足够秩。

### 6.3 半径的处理

若标准球具有可信的半径检定值，应固定 \(r_s\)，或者以检定不确定度为依据加入强先验：

\[
e_r=\frac{r_s-r_{s,\mathrm{cal}}}{\sigma_r}.
\]

若同时自由估计实体球半径、球心、手眼外参和大量 MDH 参数，模型更容易出现尺度补偿和参数相关性，不利于稳定辨识。

---

## 7. 外层虚拟球面残差

### 7.1 几何约束

若标准球通过刚性连接绕万向节固定中心运动，则各位置的实体球心 \(\mathbf c_k\) 到虚拟球心 \(\mathbf o\) 的距离应为常数 \(R_v\)：

\[
\|\mathbf c_k-\mathbf o\|_2=R_v.
\]

外层虚拟球面残差定义为

\[
e^{v}_{k}
=
\|\mathbf c_k-\mathbf o\|_2-R_v.
\tag{5}
\]

其中：

- \(\mathbf o=[o_x,o_y,o_z]^T\) 为不可直接测量的虚拟球心；
- \(R_v\) 为标准球球心到万向节转动中心的等效距离。

如果 \(R_v\) 具有可靠的机械测量值，可固定或设置先验；若难以准确测量，则将其作为一个全局隐变量估计。

### 7.2 残差雅可比

令

\[
\mathbf m_k
=
\frac{\mathbf c_k-\mathbf o}{\|\mathbf c_k-\mathbf o\|_2},
\]

则

\[
\frac{\partial e^{v}_k}{\partial\mathbf c_k}=\mathbf m_k^T,
\qquad
\frac{\partial e^{v}_k}{\partial\mathbf o}=-\mathbf m_k^T,
\qquad
\frac{\partial e^{v}_k}{\partial R_v}=-1.
\tag{6}
\]

外层约束的作用不是替代内层点云约束，而是把不同球位置的球心联系起来，使它们共享同一个运动中心和半径，从而增加跨位置的一致性信息。

---

## 8. 球心和虚拟球心作为隐变量

### 8.1 联合参数向量

建议的联合优化变量为

\[
\mathbf x=
\begin{bmatrix}
\Delta\boldsymbol\eta_{\mathrm{id}}^T &
\mathbf c_1^T & \cdots & \mathbf c_K^T &
\mathbf o^T & R_v
\end{bmatrix}^{T},
\tag{7}
\]

其中 \(\Delta\boldsymbol\eta_{\mathrm{id}}\) 只包含通过可辨识性分析保留下来的独立 MDH 参数。若需同时修正手眼外参，可在 \(\mathbf x\) 中加入六维李代数增量 \(\delta\boldsymbol\xi_{EL}\)，但不建议在缺少充分激励和约束时盲目加入。

### 8.2 为什么要联合估计

球心不是传感器直接输出量，而是由受机器人运动学参数影响的点云推断得到。因此若先使用名义机器人模型拟合球心，再把球心固定用于机器人参数辨识，将出现以下问题：

1. 球心估计中的机器人误差被固化到“观测值”中；
2. 第二阶段没有正确继承球心的不确定度；
3. 相同数据既用于生成球心，又用于评价拟合效果，可能产生自洽性偏差；
4. 串联步骤可能弱化原始点对 MDH 参数的灵敏度信息。

联合优化允许 MDH 参数、实体球心和虚拟球心相互调整，但这也带来新的风险：若球心自由度过大、姿态激励不足，它们可能吸收本应归因于机器人参数的误差。因此必须依靠多扫描姿态、外层共享约束、参数筛选和独立验证来控制这一问题。

### 8.3 自由度与规范约束

纯距离残差对整体刚体平移和旋转不敏感。如果同时自由估计基坐标系位姿、手眼位姿、全部球心和机器人全部几何参数，模型可能存在整体坐标规范自由度或近似等价参数组合。建议：

- 明确定义并固定机器人基坐标系；
- 不把任意的整体基坐标变换作为待估参数；
- 对经过独立标定且可信的手眼外参优先固定；
- 只估计具有足够观测性的 MDH 参数；
- 对必要但弱可辨识的参数使用有物理依据的先验，而不是任意强约束。

---

## 9. 两阶段初始化

联合非线性优化对初值敏感。推荐使用“两阶段初始化、最终联合优化”的流程。

### 9.1 阶段一：各实体球球心初始化

先使用名义运动学参数 \(\boldsymbol\eta_0\) 和初始手眼外参，将每个球位置的点云转换到基坐标系。对于第 \(k\) 个位置，先计算代数初值，再使用几何距离精化。

球面展开式为

\[
\mathbf p_j^T\mathbf p_j-2\mathbf c^T\mathbf p_j
+\mathbf c^T\mathbf c-r_s^2=0.
\]

若半径 \(r_s\) 固定，可通过不同点方程相减或线性最小二乘获得球心初值 \(\mathbf c_k^{(0)}\)。随后求解

\[
\mathbf c_k^{(1)}
=
\arg\min_{\mathbf c_k}
\sum_{i,j}
\rho\!\left(
\frac{\|{}^{B}\mathbf p_{kij}(\boldsymbol\eta_0)-\mathbf c_k\|-r_s}
{\sigma_{s,kij}}
\right),
\tag{8}
\]

其中 \(\rho(\cdot)\) 可选 Huber 损失，\(\sigma_{s,kij}\) 表示该点的等效径向标准差。

### 9.2 阶段二：虚拟球面初始化

使用已获得的 \(\mathbf c_k^{(1)}\) 拟合外层虚拟球面：

\[
(\mathbf o^{(0)},R_v^{(0)})
=
\arg\min_{\mathbf o,R_v}
\sum_{k=1}^{K}
w_{v,k}
\left(\|\mathbf c_k^{(1)}-\mathbf o\|-R_v\right)^2.
\tag{9}
\]

可先用线性代数球拟合获得初值，再用几何距离拟合精化。外层权重应考虑不同实体球心的拟合协方差，而不应默认所有球心同精度。

### 9.3 参数初值

令

\[
\Delta\boldsymbol\eta^{(0)}=\mathbf 0.
\]

若名义模型误差较大，可先固定球心做一次小范围 MDH 更新，再进入全部变量的联合优化；也可以采用分块迭代：固定 MDH 更新球心，固定球心更新 MDH，待进入收敛域后再全部释放。分块过程仅用于改善初值，最终结果仍应来自同一联合目标函数。

---

## 10. MDH 参数可辨识性分析

### 10.1 必要性

机器人几何参数并非天然全部独立可辨识。相邻平行或近似平行关节轴会造成部分 MDH 参数强相关；球面距离测量只直接观测法向距离，也会弱化某些方向的信息。若把全部参数直接放入优化，可能出现病态法方程、参数数值异常而残差仍很小、不同数据集得到不同参数等问题。

### 10.2 灵敏度矩阵

在初始值附近将全部白化残差堆叠为

\[
\mathbf r(\mathbf x+\Delta\mathbf x)
\approx
\mathbf r(\mathbf x)+\mathbf J\Delta\mathbf x,
\]

其中

\[
\mathbf J=\frac{\partial\mathbf r}{\partial\mathbf x}
\]

为联合雅可比矩阵。若只分析 MDH 参数，应从联合问题中消去或边缘化球心等隐变量，而不是仅查看固定球心时的 MDH 子矩阵。设

\[
\mathbf J=[\mathbf J_{\eta}\ \mathbf J_z],
\]

其中 \(\mathbf J_z\) 对应全部隐变量，则消去隐变量后的 MDH 信息矩阵可写为 Schur 补形式：

\[
\mathbf H_{\eta}^{\mathrm{red}}
=
\mathbf J_{\eta}^{T}\mathbf W\mathbf J_{\eta}
-\mathbf J_{\eta}^{T}\mathbf W\mathbf J_z
(\mathbf J_z^{T}\mathbf W\mathbf J_z)^{-1}
\mathbf J_z^{T}\mathbf W\mathbf J_{\eta}.
\tag{10}
\]

该矩阵更真实地反映了在允许球心共同调整时，MDH 参数还剩多少独立信息。

### 10.3 SVD/QR 筛选

对尺度归一化后的约化灵敏度矩阵或信息矩阵进行奇异值分解：

\[
\widetilde{\mathbf J}_{\eta}
=\mathbf U\boldsymbol\Sigma\mathbf V^T.
\]

检查：

- 数值秩；
- 最小奇异值；
- 条件数 \(\kappa=\sigma_{\max}/\sigma_{\min}\)；
- 右奇异向量对应的弱可辨识参数组合；
- 参数相关系数矩阵。

然后使用列主元 QR、SVD 截断或基于物理结构的参数合并，选择独立参数集 \(\Delta\boldsymbol\eta_{\mathrm{id}}\)。角度参数和长度参数量纲不同，应先按预期尺度归一化，例如将长度以毫米、角度以弧度的合理先验尺度进行无量纲化，避免条件数被单位选择主导。

### 10.4 实验设计评价

在正式采集前，可用候选机器人姿态和球位置计算预测雅可比，以 D-optimality、最小奇异值或条件数为指标选择姿态。目标不是单纯增加点数，而是增加相互独立的参数信息。大量近似重复的点通常不如少量几何分布良好的姿态有效。

---

## 11. 加权 LM 联合辨识

### 11.1 联合目标函数

定义内层和外层白化残差：

\[
\widetilde e^{s}_{kij}
=\frac{e^{s}_{kij}}{\sigma_{s,kij}},
\qquad
\widetilde e^{v}_{k}
=\frac{e^{v}_{k}}{\sigma_{v,k}}.
\]

联合目标函数为

\[
\boxed{
\min_{\mathbf x}
\left[
\sum_{k=1}^{K}\sum_{i=1}^{M_k}\sum_{j=1}^{N_{ki}}
\omega_{ki}\,\rho_s\!\left(\widetilde e^{s}_{kij}\right)
+\lambda_v\sum_{k=1}^{K}
\rho_v\!\left(\widetilde e^{v}_{k}\right)
+\|\mathbf L(\mathbf x-\mathbf x_{\mathrm{prior}})\|_2^2
\right]
}
\tag{11}
\]

其中：

- \(\omega_{ki}\) 用于平衡不同扫描帧的点数，防止点数多的单帧支配优化；
- \(\sigma_{s,kij}\) 表示点的径向测量不确定度；
- \(\sigma_{v,k}\) 表示第 \(k\) 个球心在虚拟球径向方向上的不确定度；
- \(\lambda_v\) 用于内外层相对权重的调节；
- \(\rho_s,\rho_v\) 为稳健损失函数；
- 最后一项为可选的物理先验或正则项。

若权重均由合理的协方差白化得到，内外层残差已经无量纲，原则上可令 \(\lambda_v=1\)。若只能使用经验权重，应进行灵敏度分析，报告不同 \(\lambda_v\) 下结果是否稳定，避免为了获得较小验证误差而事后调权。

### 11.2 外层残差权重的传播

由内层球面拟合可得到球心协方差近似值 \(\boldsymbol\Sigma_{c_k}\)。外层残差沿径向 \(\mathbf m_k\) 的方差可近似为

\[
\sigma_{v,k}^2
\approx
\mathbf m_k^T\boldsymbol\Sigma_{c_k}\mathbf m_k
+\sigma_{\mathrm{model},v}^2,
\tag{12}
\]

其中 \(\sigma_{\mathrm{model},v}\) 用于描述万向节偏心、连接杆弹性和虚拟轨迹非理想球面等模型误差。联合优化时更严格的做法是直接保留原始点残差和隐变量，由联合 Hessian 自然传播不确定性；式（12）主要用于初始化和权重估计。

### 11.3 LM 迭代

在第 \(t\) 次迭代，将经过稳健权重和测量权重处理后的残差、雅可比分别记为 \(\mathbf r_t\) 和 \(\mathbf J_t\)。LM 增量满足

\[
\left(\mathbf J_t^T\mathbf W_t\mathbf J_t
+\mu_t\mathbf D_t\right)\Delta\mathbf x_t
=-\mathbf J_t^T\mathbf W_t\mathbf r_t,
\tag{13}
\]

其中 \(\mu_t\) 为阻尼系数，\(\mathbf D_t\) 可取 \(\operatorname{diag}(\mathbf J_t^T\mathbf W_t\mathbf J_t)\) 或按参数尺度设置的正定矩阵。更新为

\[
\mathbf x_{t+1}=\mathbf x_t+\Delta\mathbf x_t.
\]

若实际目标函数下降充分，则接受步长并减小 \(\mu_t\)；否则拒绝或缩短步长并增大 \(\mu_t\)。

### 11.4 参数尺度与约束

为防止毫米量级长度参数和弧度量级角度参数造成数值失衡，应令

\[
\Delta\mathbf x=\mathbf S\Delta\mathbf y,
\]

其中 \(\mathbf S\) 为参数尺度矩阵，在无量纲变量 \(\mathbf y\) 上求解。还应设置物理合理的边界，例如限制长度修正、零位修正和手眼修正的最大幅度。边界不是可辨识性不足的替代品；若参数持续顶到边界，应重新检查模型、初值和采样设计。

### 11.5 大规模求解

每个 \(\mathbf c_k\) 只与第 \(k\) 组内层点以及一个外层残差相关，联合雅可比具有明显的块稀疏结构。可使用稀疏 LM，并通过 Schur 补先消去 \(\mathbf c_k\) 等局部变量，再求全局 MDH 参数、虚拟球心和虚拟半径，以降低计算量并改善数值稳定性。

### 11.6 收敛判据

可同时使用以下判据：

- 目标函数相对变化小于阈值；
- 归一化参数增量小于阈值；
- 梯度无穷范数小于阈值；
- 达到最大迭代次数；
- 多组合理初值收敛到近似一致的解。

只满足“训练残差下降”并不能证明参数正确，还必须检查参数物理合理性、Hessian 条件数和独立验证误差。

---

## 12. 模型更新与误差补偿

### 12.1 参数更新

辨识完成后，机器人更新模型为

\[
\widehat{\boldsymbol\eta}
=\boldsymbol\eta_0+\widehat{\Delta\boldsymbol\eta}_{\mathrm{id}}.
\tag{14}
\]

未被选入可辨识集合的参数保持名义值，或按已确定的参数组合映射回模型。必须保存名义参数、修正量、单位、MDH 约定和软件版本，防止符号或单位错误。

### 12.2 两种补偿方式

**方式一：控制器运动学参数更新。** 若机器人控制器允许安全地写入几何标定参数，可在厂商规定的备份、校验和回退流程下更新参数。更新前应确认控制器使用的 DH/MDH 定义、零位符号和长度单位与辨识模型完全一致。

**方式二：离线关节指令补偿。** 若无法修改控制器内部模型，则对期望末端位姿 \(\mathbf T_d\) 使用标定后的模型求解

\[
\mathbf q_{\mathrm{corr}}
=
\operatorname{IK}(\mathbf T_d;\widehat{\boldsymbol\eta}),
\tag{15}
\]

并将 \(\mathbf q_{\mathrm{corr}}\) 发送给机器人。若原程序给出名义关节量 \(\mathbf q_0\)，可以先根据任务定义得到目标位姿，再用标定模型反解，而不宜简单把所有已辨识参数机械地加到关节角上。

对连续轨迹还应保证逆解分支一致、关节速度和加速度连续，并检查奇异区、碰撞和关节限位。

### 12.3 局部增量补偿

对于小误差，也可在当前关节位置附近使用标定模型的末端位姿误差和机器人雅可比计算关节增量：

\[
\Delta\mathbf q
\approx
-\mathbf J_q^{\dagger}\Delta\boldsymbol\xi,
\]

其中 \(\Delta\boldsymbol\xi\) 为期望与预测末端位姿之间的六维小位姿误差，\(\mathbf J_q^{\dagger}\) 为带阻尼的广义逆。该方法适用于小范围修正；大范围运动更适合直接使用标定模型重新求逆运动学。

---

## 13. 独立验证方案

### 13.1 必须避免的验证方式

不能只用参与联合优化的点云评价补偿效果。训练数据上的内层或外层残差变小，只能证明模型更贴合这批数据，不能独立证明机器人的绝对定位精度提高。

### 13.2 推荐的验证层级

1. **留出扫描姿态验证**：在已知球位置下，使用未参与辨识的机器人姿态扫描；
2. **留出球位置验证**：使用外层虚拟球面上未参与辨识的新球位置；
3. **工作空间外推验证**：在标定采样区域之外但仍属于目标工作空间的位置验证；
4. **外部仪器验证**：优先使用激光跟踪仪、摄影测量系统、三坐标设备或其他与标定链独立的高精度测量手段；
5. **任务级验证**：测试实际路径、孔位、轮廓扫描或装配任务的误差。

### 13.3 建议报告的指标

- 内层实体球径向残差的均值、标准差、RMSE、95% 分位数和最大绝对值；
- 外层虚拟球径向残差的相同统计量；
- 不同扫描姿态拟合出的球心离散度；
- 补偿前后 TCP 三维位置误差及方向误差；
- 补偿前后验证轨迹的平均误差、RMSE、最大误差和重复性；
- MDH 参数估计值、标准差、相关系数和置信区间；
- 多次重复实验和不同初值下结果的一致性。

建议同时报告训练集与验证集结果，并将“重复定位精度”和“绝对定位精度”分开。几何标定主要改善系统性的绝对误差，通常不能消除随机重复性误差。

### 13.4 消融实验

为证明外层虚拟球面约束的实际贡献，至少比较：

- 仅使用内层实体球面约束；
- 先拟合球心再串联辨识；
- 内外层联合优化；
- 联合优化但不使用稳健损失或不使用协方差权重。

应在相同的独立验证集上比较，而不是仅比较训练目标函数。

### 13.5 与现有棋盘格眼在手上实验的统一比较口径

本工作区 `src/2025_12/calib_data_bz/trial01/` 的45组棋盘格数据已固定划分为30组训练、15组验证。旧手眼矩阵在15组验证集上的平移一致性RMSE为0.709 mm、旋转一致性RMSE为0.2848°；新手眼矩阵对应0.785 mm和0.2689°。这些数值使用各模型训练子集推定的固定棋盘格参考位姿，不能称为独立绝对TCP定位误差，也不能与实体球/虚拟球的径向残差直接比较。

双球面模型拟合后，应冻结参数，在**相同原始观测和关节角**上并排计算：

| 模型 | 机器人正运动学 | 手眼外参 | 用途 |
| --- | --- | --- | --- |
| A 原始基准 | 名义URDF/MDH | 旧 `T_flange_camera_optical` | 未进行本轮机器人补偿 |
| B 仅运动学补偿 | 双球面辨识后的MDH | 与A完全相同 | 单独衡量机器人参数修正 |
| C 联合方案（可选） | 双球面辨识后的MDH | 另行标定或联合优化的新外参 | 单独报告两种修正的合计效果 |

棋盘格观测使用 `T_world_board(i) = FK_model(q_i) × T_flange_camera_optical × T_camera_board(i)`。现有 `error.py` 从样本中直接读取采集时保存的 `robot_pose`，并不会根据候选MDH重算FK；**不能只修改MDH文件然后原样重跑 `error.py`**。后续评估器必须对每个候选模型从同一条 `joint_state.positions_rad` 重算 `T_world_flange`，保留同一图像、PnP、相机内参、原点定义和训练/验证索引。若有外部测量的固定棋盘格世界位姿或TCP真值，应对所有模型使用同一个独立参考；若没有，只能比较跨姿态一致性，不能证明绝对定位精度。

双球面自身的内层实体球径向残差和外层虚拟球径向残差，分别在留出的扫描姿态、留出的球位置报告均值、RMSE、95%分位数和最大绝对值。内层检查点是否贴合已知半径的实体球；外层检查球心轨迹是否贴合由训练集确定的虚拟球心和半径。两类数值即使都用毫米，也不应相互比大小以判断TCP精度。留出球位置的球心若需由扫描估计，应使用独立于评分扫描的测量或再分割扫描；不得用评分数据同时拟合其球心并以同一批点的残差自证。

最终机器人性能应在新的、未用于双球面参数选择的姿态和球位置上，优先由激光跟踪仪、三坐标等独立设备测量TCP实际位姿，分别比较位置MAE/RMSE/95%分位数/最大值、方向误差以及重复定位离散度。现有棋盘格15组已用于选择/评价手眼结果，可作诊断复核；若据其调双球面模型，就应另采从未查看的新姿态作为最终验证。长度类MDH修正量与角度类修正量不能按原始数字比大小；应通过验证姿态上的FK灵敏度换算为末端位姿影响，并检查参数相关性和可辨识性。

---

## 14. 主要误差来源及传播路径

### 14.1 线激光测量误差

- 传感器内部标定误差；
- 深度方向与横向误差的各向异性；
- 标准球表面反光、散斑和饱和导致的系统偏差；
- 球面边缘入射角过大导致的点偏移；
- 轮廓提取、亚像素定位和异常点误差。

这些误差首先影响 \({}^{L}\mathbf p_{kij}\)，再通过刚体变换进入球面径向残差。应依据传感器方向性噪声建立点协方差，并将其投影到球面法向，而非一律使用相同权重。

### 14.2 手眼外参误差

\({}^{E}\mathbf T_L\) 的旋转误差会随扫描点到传感器原点的距离放大，平移误差则直接传递到基坐标系点。手眼外参与机器人部分几何参数可能互相补偿。若固定手眼外参，应评估其不确定度；若联合估计，应确保采样姿态足够丰富并分析参数相关性。

### 14.3 机器人本体误差

- MDH 几何参数偏差；
- 关节零位偏差；
- 减速器回差、摩擦和迟滞；
- 连杆柔性、负载变形；
- 关节温升和环境温漂；
- 编码器噪声和控制跟随误差。

双球面几何模型主要辨识稳定的几何系统误差。方向相关的回差、负载相关变形和温漂若不单独建模，可能被错误地吸收到 MDH 参数中，因此采集时应控制运动方向、负载和热状态，并在验证中改变这些条件检查模型稳健性。

### 14.4 标准球与万向节误差

- 标准球半径误差、球度误差和表面缺陷；
- 标准球安装偏心或连接松动；
- 万向节实际转动中心漂移；
- 球心轨迹并非理想球面；
- 不同万向节姿态下的重力挠曲。

若外层残差呈现与姿态相关的系统模式，不能仅通过增大权重迫使其成为球面。应考虑降低外层权重、增加模型误差项，或建立偏心/挠曲扩展模型。

### 14.5 同步与数据关联误差

机器人关节角与激光数据若不同步，运动采集时会产生与速度相关的空间误差。优先使用静态采集或硬件触发；动态扫描时应进行时间偏差标定，并确保点云被分配到正确的球位置和机器人姿态。

---

## 15. 关键实验风险与控制措施

| 风险 | 可能后果 | 控制措施 |
|---|---|---|
| 单次只扫描很小球冠 | 球心沿视线方向不稳定 | 增加球面覆盖角和多方位扫描，检查球心协方差 |
| 机器人姿态变化不足 | MDH 参数秩亏或高度相关 | 以 SVD/条件数设计姿态，扩大关节激励范围 |
| 外层球心位置集中 | 虚拟球心、半径不稳定 | 在安全范围内增加虚拟球面角度覆盖，避免近似共圆 |
| 每帧点数差异过大 | 密集帧支配优化 | 按帧归一化或分层加权，而非单纯逐点等权 |
| 球心被当作无误差真值 | 低估参数不确定度、产生偏差 | 两阶段只用于初始化，最终保留球心为隐变量联合优化 |
| 隐变量吸收机器人误差 | 训练残差小但参数错误 | 多姿态共视同一球、外层共享约束、参数筛选和独立验证 |
| 手眼与 MDH 参数耦合 | 多组参数均可解释数据 | 优先独立高精度手眼标定，或联合可辨识性分析 |
| 使用同一数据验证 | 形成自洽性结论 | 预留新姿态、新球位置，最好使用独立外部测量系统 |
| 异常点和反光偏差 | 球面拟合被拉偏 | 预处理、质量阈值、Huber/Cauchy 损失和残差图检查 |
| 温度、负载、回差不一致 | 模型跨工况失效 | 预热、固定负载和趋近方向，并开展跨工况验证 |
| MDH 定义或单位不一致 | 补偿方向错误甚至引发风险 | 在仿真和小范围低速试验中核对坐标、符号、单位与参数顺序 |

---

## 16. 推荐的完整实施流程

1. 检定标准球半径和球度，检查万向节刚度与重复性；
2. 完成线激光内部标定和手眼外参标定，记录其不确定度；
3. 依据名义模型生成候选机器人姿态和球位置；
4. 通过预测灵敏度矩阵筛选具有较好秩和条件数的实验构型；
5. 预热机器人，固定负载、速度和趋近方向，静态或同步采集关节角与点云；
6. 进行点云质量控制，剔除明显饱和点、边缘伪点和错误关联点；
7. 用名义运动学和手眼外参将点云初步转换到基坐标系；
8. 对每个球位置进行固定半径实体球拟合，获得 \(\mathbf c_k^{(0)}\) 及协方差；
9. 用全部初始实体球心拟合 \(\mathbf o^{(0)}\) 和 \(R_v^{(0)}\)；
10. 构造内层、外层白化残差及联合雅可比；
11. 通过 SVD、列主元 QR、相关性和 Schur 补信息矩阵选择可辨识 MDH 参数；
12. 采用带稳健损失、合理权重和参数尺度的稀疏 LM 联合优化；
13. 检查收敛性、残差分布、参数物理意义、相关性和多初值一致性；
14. 更新机器人运动学模型，或利用标定模型重新求解关节指令；
15. 在独立姿态、独立球位置和任务路径上验证补偿效果；
16. 开展消融、重复性、温度和负载实验，给出不确定度与适用范围。

---

## 17. 结果判定原则

双球面联合优化是否有效，不应只依据目标函数下降，而应同时满足：

1. 保留验证集上的 TCP 或任务误差显著降低；
2. 多次采集、不同合理初值下的 MDH 修正量保持稳定；
3. 约化信息矩阵不存在未处理的明显秩亏；
4. 估计参数在机械结构和制造公差范围内具有合理性；
5. 补偿效果在目标工作空间、温度和负载范围内具有可重复性；
6. 相比仅内层约束和串联拟合，双球面联合模型在独立数据上确有增益；
7. 结果不依赖少数异常姿态、特定权重或过强先验。

---

## 18. 方法总结

该方法的本质是把机器人运动学误差转化为两个层级的几何一致性误差：第一层要求不同机器人姿态下测得的原始激光点共同落在已知半径的实体球面上；第二层要求各实体球球心共同落在同一虚拟球面上。由于实体球心和虚拟球心不能直接测量，它们应被视为隐变量。两次独立球拟合适合生成初值，但最终参数应由包含原始点、全部实体球心、虚拟球心、虚拟半径和可辨识 MDH 修正量的统一目标函数求得。

方案成功的关键不在于点云数量本身，而在于几何覆盖、机器人姿态激励、参数可辨识性、合理权重和真正独立的验证。只有当补偿后的模型能在未参与拟合的新姿态、新球位置或外部测量数据上稳定降低误差，才能证明双球面标定实现了有效的机器人运动学误差补偿。
